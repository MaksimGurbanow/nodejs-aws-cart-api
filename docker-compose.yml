services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "4000:4000"
    working_dir: /app
    user: root
    env_file:
      - .env
    environment:
      - NODE_ENV=production
